import { Request, Response } from "express";

// Stub controller for future "explain more" functionality.
const explainProjectController = async (_req: Request, _res: Response) => {
  // intentionally empty
};

export default explainProjectController;

